using UnityEngine;

public class StartGame : GameManager
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        ActivateGameplayState();
    }

    // Update is called once per frame
    void Update()
    {
        
        
    }

}
